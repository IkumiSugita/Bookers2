<% if @newbook.errors.any? %>
 <%= @newbook.errors.count %>errors prohibited this obj from being saved:
 <% @newbook.errors.full_messages.each do |message| %>
  <%= message %>
 <% end %>
<% end %>
<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
<h2>User info</h2>
<%= attachment_image_tag @book, :profile_image, :fill,60,60,fallback: "no_image.jpg"%>
<table class="table">
 <tbody>
  <tr>
   <th>name</th><th><%= @book.name %></th>
  </tr>
  <tr>
   <th>introduction</th><th><%= @book.introduction %></th>
  </tr>
 </tbody>
</table>
<%= link_to edit_user_path(@book), class: "btn btn-outline-secondary btn-block fas fa-user-cog" do %>
<% end %>
<h2 class="mt-3">New book</h2>
 　 <form action="/books" accept-charset="UTF-8" method="post">
    
     <%= form_with model: @newbook, local:true do |f| %>
     <div class from="from-group">
      <h4>Title</h4>
      <%= f.text_field :title %>
     </div>
     <div class from="from-group">
      <h4>Opinion</h4>
      <%= f.text_area :body %>
     </div>
     <div class from="from-group">
     <%= f.submit 'Create Book' %>
     </div>
     <% end %>
    </from>
   </div>
    <div class="col-md-8 offset-md-1">
     <table class="table table-hover table-inverse">
      <h2>Books</h2>
      <h4>Title</h4>
      <h4>Opinion</h4>
      <% @books.each do |book| %>
      <%= link_to user_path(book.user) do %>
      <span><%= attachment_image_tag book.user, :profile_image, :fill,60,60,fallback: "no_image.jpg"%></span>
      <% end %>
      <span><%= link_to book.title, book_path(book.id) %></span>
      <span><%= book.body %></span>
      
      <% end %>
     </table>
    </div>
  </div>
</div>